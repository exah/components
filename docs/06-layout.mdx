---
name: Layout
route: /layout
menu: Layout
---

import { Playground } from 'docz'
import { PropsTable } from '../.internal/props-table'
import { Box, Layout, Media } from '../src'

# Layout

## Description

Implements [Holy Grail Layout](https://en.wikipedia.org/wiki/Holy_grail_(web_design)) with [`FlexBox`](#/flex-box).

- Header is on top of page
- Footer is stick to "bottom" of page
- Content expands to take available space

Components:

- `Layout` — Container that takes space of page (add `minHeight='100vh'` prop, default to `100%`)
- `Layout.Content` — Main component, that expands to take available space

## Usage

```js
import { Layout, Box } from 'pss-components'
```

```js
<Layout>
  <Box>Top</Box>
  <Layout.Content>Content</Layout.Content>
  <Box>Bottom</Box>
</Layout>
```

## Examples

### Basic

<Playground style={{ height: '500px', overflow: 'auto' }}>
  <Layout>
    <Box as='header'>
      Header
    </Box>
    <Layout.Content as='main'>
      Main
    </Layout.Content>
    <Box as='footer'>
      Footer
    </Box>
  </Layout>
</Playground>

### All components

<Playground style={{ height: '500px', overflow: 'auto' }}>
  <Layout outline='1px solid' p={1}>
    <Box outline='debug' p={1} mb={1}>
      Header
    </Box>
    <Box outline='debug' p={1} mb={1}>
      Some / Breadcrumbs / Path
    </Box>
    <Media outline='debug' p={1} mb={1}>
      <Media.Content outline='debug' p={1} mx={1}>
        Content
      </Media.Content>
      <Media.Side outline='debug' p={1} width={( 1 / 6 )} order={-1}>
        Left Nav
      </Media.Side>
      <Media.Side outline='debug' p={1} width={( 1 / 6 )}>
        Right Nav
      </Media.Side>
    </Media>
    <Box outline='debug' p={1}>
      Footer
    </Box>
  </Layout>
</Playground>
