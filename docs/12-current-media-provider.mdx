---
name: CurrentMediaProvider
route: /current-media-provider
menu: Helpers
---

import { Playground } from 'docz'
import { CurrentMediaProvider, CurrentMediaConsumer, Box } from '../src'

# CurrentMediaProvider

## Description

Watch `media` defined in `theme` for changes and pass currently active media as `currentMediaKey` prop.


## Usage

```js
import { CurrentMediaProvider, CurrentMediaConsumer, withCurrentMedia } from 'pss-components'
```

```js
import { ThemeProvider } from 'emotion-theming'

<ThemeProvider theme={{ media: { sm: '(max-width: 600px)' } }}>
  <CurrentMediaProvider>
    {/* components */}
  </CurrentMediaProvider>
</ThemeProvider>
```

```js
import { withCurrentMedia } from 'pss-components'

const MediaComponent = withCurrentMedia(Component)
```

## Examples

With values from `media` prop:

<Playground>
  <CurrentMediaProvider media={{ mobile: '(max-width: 600px)', desktop: '(min-width: 601px)' }}>
    <CurrentMediaConsumer>
      {(state) => (
        <Box>
          Current media: {state.currentMediaKey}
        </Box>
      )}
    </CurrentMediaConsumer>
  </CurrentMediaProvider>
</Playground>

From `theme`:

<Playground>
  <CurrentMediaProvider>
    <CurrentMediaConsumer>
      {(state) => (
        <Box>
          Current media: {state.currentMediaKey}
        </Box>
      )}
    </CurrentMediaConsumer>
  </CurrentMediaProvider>
</Playground>

