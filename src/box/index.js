import {
  space,
  sizes,
  colors,
  utilityPropStyles
} from 'pss'

import styled from 'react-emotion'

const Box = styled('div')(
  space,
  sizes,
  colors,
  utilityPropStyles
)

export {
  Box
}
