---
name: FlexGrid
route: /flex-grid
order: -5
---

import { Playground } from 'docz'
import { ThemeProvider } from 'emotion-theming'
import { PropsTable } from '../props-table'
import { ThemeDefaultsProvider } from '../theme-defaults'
import { Box } from '../box'
import { FlexGrid } from './index'


# FlexGrid

## Description

Grid component based on [`FlexBox`](#/flex-box).

- Default to `12` columns. Can be defined in `theme.grid`
- Item size conrolled with `col` or `size` prop in `FlexGrid.Item` component
- Space between items conrolled with `space`, `spacex` and `spacey` props

### Components:


#### `FlexGrid` — Container component. Control space between items with props:

- Possible values in `theme.space`. See [space docs](https://github.com/exah/pss/blob/master/docs/api.md#space)
- `space` — horizontal and vertical space
- `spacex` — horizontal space
- `spacey` — vertical space

#### `FlexGrid.Item` — Item / column component, size can be set with props:

- `column` — Number in range `1...theme.grid`
- `size` — Number in range `0...1`, result is percentage (`0.5` is `50%`)
- `offset` — Number in range `1...theme.grid` — move item by number of columns


## Usage

```js
import { FlexGrid } from 'pss-components'
```

```js
const theme = {
  default: {
    FlexGrid: {
      grid: 12,
      space: 1
    }
  }
}
```

```js
<ThemeProvider theme={theme}>
  <FlexGrid>
    <FlexGrid.Item column={6}>
      <Box>
        Content
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={4}>
      <Box>
        Content
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box>
        Content
      </Box>
    </FlexGrid.Item>
  </FlexGrid>
</ThemeProvider>
```


## Examples

### Basic

<Playground>
  <FlexGrid spacex={1}>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
  </FlexGrid>
</Playground>

### Vertical and horizontal space between items

<Playground>
  <FlexGrid space={1}>
    <FlexGrid.Item column={1}>
      <Box outline='1px solid' pd={1}>
        1
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={2}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={3}>
      <Box outline='1px solid' pd={1}>
        3
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={6}>
      <Box outline='1px solid' pd={1}>
        6
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={12}>
      <Box outline='1px solid' pd={1}>
        12
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={4}>
      <Box outline='1px solid' pd={1}>
        4
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={4}>
      <Box outline='1px solid' pd={1}>
        4
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={4}>
      <Box outline='1px solid' pd={1}>
        4
      </Box>
    </FlexGrid.Item>
  </FlexGrid>
</Playground>

### Offset items

<Playground>
  <FlexGrid space={1}>
    <FlexGrid.Item column={3} mgr='75%'>
      <Box outline='1px solid' pd={1}>
        3
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={3} offset={3}>
      <Box outline='1px solid' pd={1}>
        3
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={3} offset={6}>
      <Box outline='1px solid' pd={1}>
        3
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item column={3} offset={9}>
      <Box outline='1px solid' pd={1}>
        3
      </Box>
    </FlexGrid.Item>
  </FlexGrid>
</Playground>

### Dynamic size

<Playground>
  <FlexGrid space={1}>
    <FlexGrid.Item size={1 / 12}>
      <Box outline='1px solid' pd={1}>
        1
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={2 / 12}>
      <Box outline='1px solid' pd={1}>
        2
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={3 / 12}>
      <Box outline='1px solid' pd={1}>
        3
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={6 / 12}>
      <Box outline='1px solid' pd={1}>
        6
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={12 / 12}>
      <Box outline='1px solid' pd={1}>
        12
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={4 / 12}>
      <Box outline='1px solid' pd={1}>
        4
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={4 / 12}>
      <Box outline='1px solid' pd={1}>
        4
      </Box>
    </FlexGrid.Item>
    <FlexGrid.Item size={4 / 12}>
      <Box outline='1px solid' pd={1}>
        4
      </Box>
    </FlexGrid.Item>
  </FlexGrid>
</Playground>

### Set defaults in `theme`

```js

const theme = {
  default: {
    FlexGrid: {
      grid: 16,
      space: 1,
      justifyContent: 'flex-end'
    }
    // other defaults
  }
  // rest is the same
}
```

[`ThemeDefaultsProvider`](#/theme-defaults-provider) can be used to change defaults in runtime:

<Playground>
  <ThemeDefaultsProvider 
    FlexGrid={{ grid: 16, space: 1, justifyContent: 'flex-end' }}
  >
    <FlexGrid>
      <FlexGrid.Item column={1}>
        <Box outline='1px solid' pd={1}>
          1
        </Box>
      </FlexGrid.Item>
      <FlexGrid.Item column={2}>
        <Box outline='1px solid' pd={1}>
          2
        </Box>
      </FlexGrid.Item>
      <FlexGrid.Item column={3}>
        <Box outline='1px solid' pd={1}>
          3
        </Box>
      </FlexGrid.Item>
      <FlexGrid.Item column={4}>
        <Box outline='1px solid' pd={1}>
          4
        </Box>
      </FlexGrid.Item>
      <FlexGrid.Item column={6}>
        <Box outline='1px solid' pd={1}>
          6
        </Box>
      </FlexGrid.Item>
      <FlexGrid.Item column={16}>
        <Box outline='1px solid' pd={1}>
          16
        </Box>
      </FlexGrid.Item>
      <FlexGrid.Item column={8}>
        <Box outline='1px solid' pd={1}>
          8
        </Box>
      </FlexGrid.Item>
    </FlexGrid>
  </ThemeDefaultsProvider>
</Playground>


## Props

### FlexGrid

<PropsTable of={FlexGrid} />

### FlexGrid.Item

<PropsTable of={FlexGrid.Item} />
