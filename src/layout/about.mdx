---
name: Layout
route: /layout
order: -4
---

import { Playground } from 'docz'
import { Layout } from './index'

# Layout

## Description

Implements [Holy Grail Layout](https://en.wikipedia.org/wiki/Holy_grail_(web_design)) with [`FlexBox`](#/flex-box).

- Header is on top of page
- Footer is stick to "bottom" of page
- Content expands to take available space
- Body can be used as container to equal height columns (like navs) and main Content

Components:

- `Layout` — Container that takes space of page (add `minHt='100vh'` prop, default to `100%`)
- `Layout.Content` — Main component, that expands to take available space
- `Layout.Body` — Can be used inside `Layout` as main container of vertical columns and content
- `Layout.Item` — Basic component that can be used for horizontal sections inside `Layout` or vertical in `Layout.Body`

## Usage

```js
import { Layout } from 'pss-components'
```

```js
<Layout>
  <Layout.Item>Top</Layout.Item>
  <Layout.Body>
    <Layout.Content>Content</Layout.Content>
    <Layout.Item>Side Nav</Layout.Item>
  </Layout.Body>
  <Layout.Item>Bottom</Layout.Item>
</Layout>
```

## Examples

### Basic

<Playground style={{ height: '500px' }}>
  <Layout>
    <Layout.Item comp='header'>
      Header
    </Layout.Item>
    <Layout.Content comp='main'>
      Main
    </Layout.Content>
    <Layout.Item comp='footer'>
      Footer
    </Layout.Item>
  </Layout>
</Playground>

### All components

<Playground style={{ height: '500px' }}>
  <Layout bg='gray.0' pd>
    <Layout.Item bg='gray.1' pd mgb>
      Header
    </Layout.Item>
    <Layout.Item bg='gray.1' pd mgb>
      Some / Breadcrumbs / Path
    </Layout.Item>
    <Layout.Body bg='gray.1' pd mgb>
      <Layout.Content bg='gray.2' pd mgx>
        Content
      </Layout.Content>
      <Layout.Item bg='gray.2' pd wd={( 1 / 6 )} order={-1}>
        Left Nav
      </Layout.Item>
      <Layout.Item bg='gray.2' pd wd={( 1 / 6 )}>
        Right Nav
      </Layout.Item>
    </Layout.Body>
    <Layout.Item bg='gray.1' pd>
      Footer
    </Layout.Item>
  </Layout>
</Playground>
